<div class="field">
	<label for="{{name}}" class="field__label">{{label}}</label>
	<input oninput="signupForm.change (this.name, this.value)"
	type="{{type}}" class="field__input" name="name" placeholder="{{placeholder}}">
</div>